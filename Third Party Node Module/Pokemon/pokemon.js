//> npm i pokemon
const pokemon = require("pokemon");

console.log(pokemon.random()); //get pokémon names randomly
console.log(pokemon.all()); //get all pokémon names

// some needed module
// nodemon -g